The remote instance is running `server.py`, your goal is to send 5 valid serials for given usernames. 
Reverse the serial algorithm from the binary `yakg` and implement the `compute_serial` function inside `client.py`.

Good luck ! :)
